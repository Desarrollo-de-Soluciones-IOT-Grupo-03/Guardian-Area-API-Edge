spring.application.name=guardian.area

# swagger-ui custom path
springdoc.swagger-ui.path=/swagger-ui.html
server.port=8081

# Spring DataSource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/guardian_area_edge?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Spring Data JPA Configuration
spring.jpa.show-sql=true

# Spring Data JPA Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# MQTT Configuration Properties
mqtt.url=ssl://655fbaf4f24549a7bbafdd4453100a04.s1.eu.hivemq.cloud:8883
mqtt.client=guardian_consumer_mqtt_client
mqtt.credentials.username=guardianarea
mqtt.credentials.password=Guardianarea219